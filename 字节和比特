#include<stdio.h>
//声明要使用这个库（函数）std-标准，i-input,o-output。
//主函数，程序入口，不允许多个
int main()//mian函数执行后返回int（整形值）
{
printf("hello\n");//这里是完成的任务，函数printf为print function（打印函数）
char ch = 'A';//分配一个符号内存
printf("%c\n", ch);//%c打印字符串格式数据
int age = 20;//分配一个整形数据内存
printf("%d\n", age);//%d打印整形十进制数
//short int 、long、long long都用%d打印数据，只是分配内存空间和最大允许长度的区别
//%d打印整型，%c打印字符，%f打印浮点数（小数），%p以地址的形式打印，%x打印16位数字.......
float f = 5.0;
printf("%f\n", f);
printf("%d\n", sizeof(char));//sizeof()函数为判断字节（如char为1字节、double为8字节）
//什么是字节？计算机常用以下大小判据：bite（比特位，最小单位），byte（字节），kb，mb，gb,tb,pb来形容大小。
//比特位是最小单位，只能存放1位2进制数（0或1），8个比特位被称为1个字节。从字节开始每次进位为1024。如1mb=1024kb。
//那么字节究竟多大？n字节能表示的排列组合量为2的n次方。如3字节能表示8个排列组合。
//3字节能表示0,1,2,3,4,5,6,7共八个，最大位为2的n次方减1。2^n-1。
return 0;
}
