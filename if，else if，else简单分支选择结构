#define _CRT_SECURE_NO_WARNINGS 1
//C语言是一门程序化的结构语言
//1、顺序结构，2、选择结构，3、循环结构
#include<stdio.h>

int main()//多分支选择结构
{
	int a = 0;
	printf("请输入您的年龄：\n");
	scanf("%d", &a);
	if (a <= 18)
	{
		printf("未成年人。\n");
	}
	//else if (18 < a <= 50)//大于等于符号为>=，注意等号放在大于号之后
	//但这是一种错误的写法，原因是容易产生歧义。
	//正确的写法需要采用计算机的逻辑判断符号。
	else if(a > 18 && a <= 30)//&&是逻辑中的并
	{
		printf("青年人。\n");
	}
	else if (a > 30 && a <= 50)//&&是逻辑中的并
	{
		printf("中年人。\n");
	}
	else
	{
		printf("老年人。\n");
	}
	//以上就是一个简单的多分支选择结构，采用的是if，if else，else来判断
	//else if可以重复使用
	//以上是一种写法，写起来代码长，但是比较直观。适用于简单的范围判断等。{}内表示代码块
	return 0;
}
