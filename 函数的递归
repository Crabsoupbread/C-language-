#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>

void print(int n)
{
	if (n > 9)//判断是否超过个位数限制
	{
		print(n / 10);//函数自己调用自己的形式被称为函数的递归。
	}
	printf("%d ", n%10);//n%10可以获取这一次递归时的最后一位数字。
}
//这一代码块的意思是，当想要顺序打印一个数时，将数字拆分会更加方便。
//print写在print函数中时，满足if语句就会重新调用这一函数。此程序不断调用直到获取到各位数字后，再一步步往回走。
//最后结束调用，就能顺序打印出来。

int main()
{
	unsigned int num = 0;
	scanf("%d", &num);
	print(num);
	
	return 0;
}
//递归只需要少量的程序就能完成巨大的计算量
//递归在书写的过程中必须给出一个条件（如本例的if），否则函数不断递归会导致栈溢出。
//递归的要求：1.存在限制条件，满足条件则递归终止。2.每次调用之后都接近它的限制条件
