#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>

int main()
{
	//整形提升的意思是，计算时提升字节数，出结果再截断。
	//整形提升是按照变量的符号位去提升的。
	char a = 3;//char是个单字节，8个bit位
	//00000011
	char b = 127;
	//01111111
	char c = a + b;
	//整形提升：
	//00000000000000000000000000000011 -a
	//00000000000000000000000001111111 -b
	//00000000000000000000000010000010 -c = a+b
	//截断：
	//10000010 -c
	//char是个有符号形式，最高位为符号位。因此c被判断为负数。
	printf("%d\n", c);
	//整形提升：（重复最高位数）
	//11111111111111111111111110000010 -c
	//负数储存的是补码，补码-1是反码，最后打印的是原码。
	//符号位不变。其他位按位取反。
	//11111111111111111111111110000010 - 补码
	//11111111111111111111111110000001 - 反码
	//10000000000000000000000001111110 - 原码
	//所以结果是-126
	
	return 0;
}
