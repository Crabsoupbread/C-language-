#define _CRT_SECURE_NO_WARNINGS 1

#include<stdio.h>

int main()
{
	int a = 2;
	//整形有4个字节，32个比特位，即a实际存储的二进制代码为：
	//000000000000000000000000000000010
	//<<和>>表示二进制位的左右移动（每次一位）如：
	int b = a << 1;
	int c = a >> 1;
	printf("%d\n", a);
	printf("%d\n", b);
	printf("%d\n", c);
	//这就表示b将a的1位向前移动一位，c将a的位向后移动1位。所以b = 2^2 * 1 = 4, c = 2^0 * 1 = 1。

	//二进制位操作
	//&按位与
	//|按位或
	//^按位异或
	int d = a & b;//010与100取交集，因此是000。（值为0）
	int e = a | c;//010与001取并集，因此是011。（值为3）
	int f = a ^ b;//010与100取异或，因此是110。（值为6）
	//交集和并集的区别是，交集是所有项都为1才是1，并集只要有1个1就是1。
	//异或的计算方法是，相同位对应的值不同时，值取1，相同时则取0。
	printf("%d\n", d);
	printf("%d\n", e);
	printf("%d\n", f);
	//复合赋值符：
	 a += 10;//实际就表示 a = a + 10这一表达式，将符号提前进行了简化
	 //类似的：
	 b &= 3;//表示b与3取交集，实际就等于 b & 3
	 //逻辑取反符号为！，对于一切非0数都被判定为逻辑中的真，只有0为假。
	 //sizeof()用于计算其中变量/类型所占空间的大小，单位是字节
	 //对于数组来说，计算后的大小等于数组内元素个数乘以元素性质对应的字节数，如int类型的就是4*n
	return 0;
}
